package com.example.target;

/** The other side of {@link EnhancedEntityTarget}'s association, whose method its writer calls. */
public class EnhancedAssociationTarget {
    public void addItem(Object item) {
    }
}
